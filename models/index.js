// Takes the exported content and stores it within their associated variables
const User = require('./User');
const Thought = require('./Thoughts');

// To then be exported from 'index.js'
module.exports = { User, Thought };
